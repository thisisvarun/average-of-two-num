/* A C program to find the average of two numbers*/
#include<stdio.h>
int main()
{
	int a,b;
	int c;
	printf("enter the values of a,b");
	scanf("%d%d",&a,&b);
	printf("the average of the two values is %d");
	c=a+b/2;
	return 0;
}
